import React from 'react'

const ImagesList = (props) => {
    const { images } = props;
    console.log('images', images);
    return (
        <div>
            Image List
        </div>
    )
}

export default ImagesList;
